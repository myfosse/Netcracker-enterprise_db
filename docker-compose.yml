version: '3'
services:
  db:
    container_name: postgres-db
    image: library/postgres:12-alpine
    env_file:
      - database.env
    ports:
      - 8000:5432
    volumes:
      - database-data:/var/lib/postgresql/data/

volumes:
  database-data: